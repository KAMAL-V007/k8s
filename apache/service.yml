kind: Service
apiVersion: v1
metadata:
    name: apache-service
    namespace: apache

spec:
  selector: 
      app: apache
  ports:
  - protocol: TCP
    port: 80 # port exposed by the cluster
    targetPort: 80 #container ka port
  type: ClusterIP
  

